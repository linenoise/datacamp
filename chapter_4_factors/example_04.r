survey.vector <- c("M","F","F","M","M")
factor.survey.vector <- factor( survey.vector )
factor.survey.vector #Print to console

levels(factor.survey.vector) <- c("Female", "Male") #Your code here

factor.survey.vector
